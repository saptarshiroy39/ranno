Write the full, complete, and raw Python code for the user's request.
IMPORTANT: Do NOT use ellipses (...), do NOT use placeholders, and do NOT skip any lines.
Do NOT use markdown backticks (```). Return ONLY the raw code.
